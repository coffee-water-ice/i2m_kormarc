fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
pydantic-settings==2.7.1
requests==2.33.1
beautifulsoup4==4.14.3
pymarc==5.1.0
gspread==6.2.1
oauth2client==4.1.3
pandas==3.0.2
openpyxl==3.1.5
openai==2.33.0
python-dotenv==1.2.2
google-auth
streamlit>=1.32

# --- 245 계열(700/900) 한자·가나 표기 변환 — core/text_utils.py에서 선택적 import ---
# 셋 다 없어도 모듈은 로드되고(try/except), 한자/가나 관련 변환만 건너뛴다.
hanja           # 한자 → 한국 음독 변환 (kanji_to_korean_reading)
pykakasi        # 일본어 한자/가나 → 히라가나 변환
jaconv          # 히라가나 ↔ 가타카나 변환

# --- 아래는 041/653 스텁을 실제 로직으로 이식할 때 추가로 필요한 의존성 ---
# httpx           # 653: 비동기 HTTP 클라이언트 (원본이 async 기반)
# tenacity        # 653: 재시도 데코레이터
